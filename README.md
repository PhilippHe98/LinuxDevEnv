# Installation
git clone https://github.com/PhilippHe98/LinuxDevEnv.git dotfiles && cd dotfiles \
chmod +x install.sh \
./install.sh

## neovim
nvim is configured thorugh the personal kickstart repository
